[toolchain]
# Must be >= `rust-version` in src-tauri/Cargo.toml, otherwise every cargo
# invocation fails the MSRV gate before it compiles anything.
channel = "1.98.1"
components = ["rustfmt", "clippy"]
