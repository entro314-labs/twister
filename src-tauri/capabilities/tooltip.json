{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tooltip",
  "description": "The tooltip window: it listens for content and reports its size.",
  "windows": ["tooltip"],
  "permissions": ["core:event:default", "allow-tooltip-ready"]
}
